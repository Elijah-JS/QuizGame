/**
 * Local-first persistence keys. Bump key or add migrations when stored shape changes.
 * Keep legacy string so existing users keep their saved deck/settings/progress.
 */
export const STUDY_COACH_STORAGE_KEY = "kine3050_state_merged_v1";
