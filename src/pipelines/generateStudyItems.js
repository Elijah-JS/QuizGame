/**
 * App entrypoint for study-item generation. Implementation lives under ./generation/.
 */
export {
  generateStudyItems,
  DEFAULT_GENERATION_MODE,
  GENERATION_MODE_RULES,
  GENERATION_MODE_LOCAL_AI,
  GENERATION_MODE_HYBRID,
} from "./generation/generateStudyItems";
